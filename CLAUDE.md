# CLAUDE.md — Help Agent Accelerator

## Git Identity

This is a **public GitHub** repository (github.com/salesforce/help-agent-accelerator). Commits must be authored with a GitHub-associated identity.

- **Name:** `Jose Carlos Diaz`
- **Email:** `645154+josecdiaz@users.noreply.github.com`

The repo-local git config is already set. If it gets reset, run:

```bash
git config user.name "Jose Carlos Diaz"
git config user.email "645154+josecdiaz@users.noreply.github.com"
```

Do **not** use the internal Salesforce email — the `salesforce-cla` check will reject commits that aren't associated with a GitHub user.
